PORT=3000
JWT_SECRET=change-me-in-production-use-long-random-string
NODE_ENV=development
GOOGLE_APPLICATION_CREDENTIALS=C:\devops project\backend\firebase.json
# Firebase Admin: paste the full JSON of your service account (single line), or use GOOGLE_APPLICATION_CREDENTIALS
# Firestore console → Project settings → Service accounts → Generate new private key
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"..."}
