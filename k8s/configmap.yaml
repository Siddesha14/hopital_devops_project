apiVersion: v1
kind: ConfigMap
metadata:
  name: hms-backend-config
  labels:
    app: hms-backend
data:
  NODE_ENV: "production"
  PORT: "3000"
  # Replace with a Kubernetes Secret in real deployments
  JWT_SECRET: "replace-with-secret-from-sealed-secrets-or-vault"
