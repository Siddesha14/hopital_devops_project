apiVersion: v1
kind: Service
metadata:
  name: hms-backend
  labels:
    app: hms-backend
spec:
  type: NodePort
  selector:
    app: hms-backend
  ports:
    - name: http
      port: 80
      targetPort: 3000
